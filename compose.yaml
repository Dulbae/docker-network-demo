services:
  web:
    image: nginx:latest
    ports:
      - "${NGINX_PORT}:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
